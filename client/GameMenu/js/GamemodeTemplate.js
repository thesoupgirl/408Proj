// A blank gamemode, to use as a template for any other gamemodes

// Do not rename the GameMode function - As this is necessary for the game engine to load
function GameMode() {
  this.start = function() {
    // Called when the game starts
  }
  this.tick = function() {
    // Called every frame update
  }
}
